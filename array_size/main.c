#include <stdio.h>
#include <stdlib.h>

int main()
{   int number[50];
    char word[20];
    double price[100];

    printf(" array size 50 ints=%d  20 char =%d   \n 100 double =%d \n",sizeof(number),sizeof(word),sizeof(price));
    return 0;
}
